---
title: "Berkley cs267(一):处理器基本架构"
description: ""
published: 2026-09-01
tags: ["cs267"]
category: "并行计算"
draft: false
---
## Idealized Uniprocessor Model
一个"真空中的球形处理器"应该能完成什么功能?

- 定义**变量**: 整数,浮点数,指针等
- 对变量进行**运算**: 算术,逻辑运算等
- 控制操作**顺序和逻辑**: 分支逻辑,循环等

理想的处理器只在运算这一步有cost,读写都是免费的.

processor的时间的处理时间量级大概在1ns,而memory的读写时间量级却在100ns.

### Compilers and assembly code
如C/C++和Fortran的编译器检查程序是否合法,然后将其翻译(并优化)为汇编代码.

### More Realistic Uniprocessor Model

内存读取的cost有两部分

- 读取会写入一个字符(不妨记延迟为$\alpha$秒)
- 存储信息的传输带宽限制(不妨记为$\beta^{-1}$ byte/sec)
则读取并传输$n$ byte信息的时间为$\alpha+n\beta$.

![alt text](image.png)

## Memory Hierarchy
大部分程序有定域性:读取的数据在时间(顺序)或空间相邻的位置.

### Cache Basics

缓存(Cache)是存储copy of data的地方.processor访问缓存快且便宜,访问非缓存数据更贵.

Cache line length:当访问Cache时必然会整块的读取,即便你只想读取其中某个地址上的数据.

### Approaches to Handling Memory Latency

- Reuse Values in fast memory
需要数据最近被读取过

- Move larger chunks (achieve higher bandwidth)

- parallel reads/writes (concurrency)

Little's law:concurrency=latency*bandwidth

## Parallelism within single processors

- Pipelining
